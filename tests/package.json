{
  "name": "lionrocket-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for Lion Rocket AI Chat Service",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:chrome": "playwright test --project=chromium",
    "test:firefox": "playwright test --project=firefox",
    "test:webkit": "playwright test --project=webkit",
    "test:mobile": "playwright test --project=\"Mobile Chrome\" --project=\"Mobile Safari\"",
    "test:api": "playwright test api/",
    "test:auth": "playwright test auth/",
    "test:chat": "playwright test chat/",
    "test:admin": "playwright test admin/",
    "report": "playwright show-report",
    "codegen": "playwright codegen",
    "install": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "faker": "^5.5.3"
  }
}